import type { InstanceOptions } from '@vtex/api';
export interface PaymentParams {
    account: string;
    paymentId: string;
    transactionId: string;
}
export declare const useSecureProxy: (baseUrl: string) => Required<InstanceOptions>['middlewares'][0];
