import type { InstanceOptions, IOContext } from '@vtex/api';
import { ExternalClient } from '@vtex/api';
export declare class SecureExternalClient extends ExternalClient {
    protected baseURL: string;
    protected context: IOContext;
    protected options?: InstanceOptions | undefined;
    constructor(baseURL: string, context: IOContext, options?: InstanceOptions | undefined);
}
